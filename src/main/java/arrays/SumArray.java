package arrays;

import java.util.Arrays;

/**
 * Created by clara on 2/8/18.
 */
public class SumArray {
    
    public static void main(String[] args) {
        
        int[] example = { 5, 6, 2, 12, 300 };
        int total = sumArray(example);
        System.out.println("The total of all the numbers in the array " + Arrays.toString(example) + " is " + total);
        
        
    }
    
    public static int sumArray(int[] array) {
        
        //todo add up all the numbers in example and return the total
        // your method should work for any array, not just the example one
        return 0;
      
    }
}
